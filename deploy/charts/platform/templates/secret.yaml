# TODO: Helm 管理 Secret 方案（后续启用）
# 当前使用外部手动创建的 forge-secrets，见 templates/deployment.yaml
#
# apiVersion: v1
# kind: Secret
# metadata:
#   name: {{ .Release.Name }}-secret
#   namespace: {{ .Release.Namespace }}
#   labels:
#     app: {{ .Release.Name }}
# type: Opaque
# stringData:
#   github-token: {{ .Values.secret.githubToken | quote }}
