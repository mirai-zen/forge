replicaCount: 1

image:
  repository: ghcr.io/mirai-zen/forge-platform
  tag: aa6c395
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8880

resources:
  requests:
    memory: "64Mi"
    cpu: "100m"
  limits:
    memory: "256Mi"
    cpu: "500m"

env:
  MYSQL_DSN: root:123456@tcp(host.docker.internal:3306)/forge_platform?charset=utf8mb4&parseTime=True&loc=Local
  GITHUB_TOKEN: ""  # 通过 K8s Secret 注入
